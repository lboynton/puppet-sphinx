#!/bin/bash 

# Managed by Puppet
# DO NOT EDIT THIS FILE!!
# All scripts can be found in /etc/sphinx.d/ - please follow 
# the same format (.source files and .index files)

for i in /etc/sphinx.d/*; do
    if [ -d "$i" ]; then
        # prioritise files which don't contain delta at the end of the filename
        for x in $i/*.source; do
            if [[ $x != *delta.source ]]
            then
                cat $x
            fi
        done
        for x in $i/*delta.source; do
            cat $x
        done
        for x in $i/*.index; do
            if [[ $x != *delta.index ]]
            then
                cat $x
            fi
        done
        for x in $i/*delta.index; do
            cat $x
        done
    fi
done

cat /etc/sphinx.d/searchd.conf
cat /etc/sphinx.d/indexer.conf